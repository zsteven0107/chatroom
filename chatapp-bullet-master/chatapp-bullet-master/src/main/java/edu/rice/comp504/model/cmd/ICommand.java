package edu.rice.comp504.model.cmd;

/**
 * The ICommand is an interface used to pass commands to objects.
 */
public interface ICommand {
    /**
     * Execute the command.
     */
    public void execute();
}
